HomeWidget Backend

This is the backend package for the HomeWidget system.

Note: The repository’s main README is at the project root. This local README exists so packaging tools can access a README within the package directory during editable installs.
