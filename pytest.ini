[pytest]
markers =
    contract: End-to-end contract tests that require a running backend instance.

# pytest-anyio importiert anyio vor dem Assert-Rewriting. Dies ist technisch unkritisch,
# da es nur die Assertion-Introspection für anyio-interne Asserts betrifft (die nicht existieren).
# Siehe: docs/dev/bekannte-warnungen.md
filterwarnings =
    ignore:Module already imported so cannot be rewritten.*anyio:pytest.PytestAssertRewriteWarning
