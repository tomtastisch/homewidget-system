name: 'Setup Backend'
description: 'Setzt Python ein, installiert Abhängigkeiten, konfiguriert pip-Caching und bereitet die Backend-Umgebung vor'
runs:
  using: "composite"
  steps:
    - name: 🐍 Python 3.13 einrichten
      uses: actions/setup-python@v5
      with:
        python-version: '3.13'
        cache: 'pip'
        cache-dependency-path: |
          backend/pyproject.toml
          backend/setup.py
          backend/requirements*.txt

    - name: 📦 Backend-Setup
      shell: bash
      run: |
        bash tools/dev/pipeline/ci_steps.sh backend_setup_env
